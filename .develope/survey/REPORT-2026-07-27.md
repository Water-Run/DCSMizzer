# DCSMizzer survey completion report — 2026-07-27

## Decision

The reproducible **static survey is complete** and is sufficient to begin
designing model-facing `Tools/` and `Docs/` without guessing what evidence
exists. It does not establish DCS runtime validity. Runtime work is excluded
because the user explicitly prohibited launching DCS.

## Evidence roots and coverage

| Label | Kind | MIZ | CMP | Coverage |
|---|---|---:|---:|---|
| `install` | DCS World installation | 648 | 5 | Built-in missions, training, campaigns, module content found under the installed tree |
| `saved` | all applicable Saved Games DCS roots | 76 | 0 | User missions found during the 2026-07-27 read-only scan |
| `official` | local official-campaign mirror | 144 | 5 | Exact content mirror of 149 install items; not counted as new unique evidence |
| `upstream` | six acknowledged upstream clones | 182 | 0 | Mission fixtures/examples present at the recorded commits |

All four roots were accessible. The scan found 1,050 MIZ instances and 10 CMP
instances, representing 904 unique MIZ contents and 5 unique CMP contents.
The report intentionally omits mission filenames, titles, briefings, absolute
paths, and per-file hashes.

## Gate results

### Gate A — corpus and archive: pass

- 1,050/1,050 MIZ archives were valid ZIP files, path-safe, and CRC-valid.
- No encrypted member, path traversal, malformed ZIP, or CRC failure was found.
- The official mirror overlap is reproducible: 149 shared contents with the
  install root.
- Upstream contains 182 MIZ instances but 180 unique contents.
- Historical archives contain 29 extra duplicate member entries across install
  and mirror copies. Duplicate entries remain visible as diagnostics.
- Nine missing historical core members are recorded without misclassifying the
  containing archives as corrupt.

### Gate B — Lua, MIZ, and CMP semantics: pass

- The project data-only parser parsed 1,050/1,050 `mission` tables without
  executing Lua.
- Saved Games coverage includes 71 UTF-8 and 5 CP1251 mission tables.
- All 10 CMP instances parsed. The five unique campaigns contain 69 stages and
  144 mission references; all references resolve in both install and mirror
  instances.
- Current semantic coverage records groups, units, routes, human slots,
  pylons/CLSIDs, triggers, task IDs, goals, warehouses, resources, briefing
  size, and modern fields such as DTC, data cartridges, datalinks, dynamic
  spawn templates, and cold-start state.
- Existing mission archives contain missing resource mappings: 78 in the
  install corpus, of which 77 are referenced and 1 is stale; the official
  subset accounts for 50 of those. Upstream examples add 3 referenced misses.
  These are source-mission fidelity findings, not parser failures.

### Gate C — upstream and reference provenance: pass

- All six clones are valid, clean Git worktrees and match the inspected remote
  branch head.
- Recorded heads:
  - BriefingRoom `a5893db7daece0e2c25403c34a104057b7365a59`
  - GTD `d58c7a38d3f0a681bde67bed21868b6d3ecd9bb8`
  - mission-maker `48b2841b4f72ba32be217f3e618cfa3cec6c8f28`
  - Retribution `e9b2387e2b45ad33ebf872623f88b40cf4d53625`
  - MOOSE `de3dc95c92c6b8f28d1037a3e7bd2d4c2b4d8655`
  - pydcs `412952c5ad5688783d8d53830280f316dbe311ff`
- License evidence is present for every clone.
- All 45 legacy reference files have SHA-256, size, source project, source
  path/glob, and frozen commit provenance; every source path exists at its
  recorded commit.
- Legacy Retribution counts are explicitly bound to
  `b7493d016f3c2c65d3a1ba73efdf0861d9c2dd7e`, not current Retribution.
- Every reference subdocument is marked as frozen historical evidence.

### Gate D — current DCS exact-data sources: pass for static coverage

- Installed executable: DCS World Steam Edition `2.9.28.26283`.
- Steam build: `24331355`; installed and target build IDs agree.
- The version matches Eagle Dynamics' 2026-07-22 release record.
- Installed module directories observed:
  - aircraft: F-16C, JF-17, M-2000C, MiG-29-Fulcrum, Su-25T, TF-51D
  - terrain: Caucasus, GermanyColdWar, Kola, Sinai
  - campaign: M2000C Red Flag
- Current `db_countries.lua` yields 92 unique identifiers with no duplicate.
- All 62 current default payload tables parse: 614 presets, 4,605 pylon
  assignments, 186 unique CLSIDs, and 14 task IDs.
- The installed tree contains 4,228 relevant Lua source files and evidence of
  unit, task, weapon, loadout, and pylon declarations.
- Exact authority boundaries:
  - country identifiers: verified current static source;
  - default payload presets: verified current static data;
  - complete unit registry and task capabilities: initialized Mission Editor
    database required;
  - complete store-to-station compatibility: initialized database required;
  - airport IDs, coordinates, runways, and parking: per-terrain runtime export
    required.
- pydcs, BriefingRoom, Retribution, and the legacy snapshots are reference
  evidence only and cannot override a version-matched DCS source.

### Gate E — runtime: accepted external boundary, not pass

The user prohibited launching DCS. Mission load, Mission Editor resave,
initialized-registry export, and per-terrain airport export therefore did not
run. No runtime-valid claim is permitted. This is a deliberate external
boundary, not a hidden success.

## Validation

- Test command:
  `python -m unittest discover -s .develope\survey -t .develope\survey -p test_*.py`
- Result: 37 tests passed.
- Full semantic command completed successfully in 110.6 seconds.
- Privacy checks found no local absolute paths or Steam owner identifiers in
  the committed machine baselines.

## Tools/Docs admission rules

Model-facing construction may now begin, subject to these non-negotiable rules:

1. expose evidence version and authority with every exact-data lookup;
2. never label a legacy or upstream value as current local DCS truth;
3. distinguish archive, parse, static, and runtime validation in APIs and prose;
4. reject or escalate exact registry, pylon compatibility, and airport parking
   requests when no version-matched runtime export exists;
5. keep generated outputs separate from installed DCS, Saved Games evidence,
   official mirrors, and upstream clones.
