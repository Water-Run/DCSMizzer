# Runtime probes

`runtime_registry_probe.lua` is a development-only aggregate probe design. It
does not export unit names, weapon names, source definitions, or third-party
content.

It was **not successfully executed** and is **not validated**. The user
explicitly prohibited launching DCS, so no automated command may deploy or run
this probe in the current survey. It remains only to document the exact
external check that would be needed if the user authorizes runtime work in a
future session.

No runtime result or product capability may depend on this file.
