= Prompt 示例
:toc: macro
:toclevels: 5

link:README-zh.md[回到 README]

这些示例展示用户可以怎样用自然语言描述想要的 DCS 模拟场景。它们不是表单，也不是固定模板；请按实际需求增删信息。

toc::[]

== 快速任务

=== 飞行与程序

==== 自由飞行

===== 日间自由飞行

[source,text]
----
生成一个高加索地图的 F-16C 日间自由飞行任务。玩家从 Batumi 热启动，沿海岸和山谷飞行约 25 分钟，不设置敌人或强制目标，天气晴朗并提供返场航路。

使用项目中实际可用的数据核实地图、机场、单位、载荷和坐标，生成对应的 .miz 并完成验证。
----

===== 夜间自由飞行

[source,text]
----
我想在波斯湾地图用 M-2000C 做一次夜间自由飞行，从 Al Dhafra 跑道起飞，经过迪拜城区和海岸后返航。使用有月光但能明显看到城市灯光的天气，不安排战斗。

不要凭记忆编造 DCS 内部名称；请生成任务文件、简报和验证结果。
----

===== 地形观光

[source,text]
----
生成一个德国冷战地图的 TF-51D 地形观光任务，从西德机场热启动，依次飞越易北河、汉堡和北海岸线。任务只用于熟悉地图，设置清晰航点和约 40 分钟的轻松航程。

请先查询真实可用的数据，生成后检查出生点、航路、载荷、触发条件和目标逻辑。
----

===== 恶劣天气飞行

[source,text]
----
做一个科拉地图的 MiG-29A 恶劣天气飞行任务。玩家冷启动后穿越低云、降雪和强侧风，在两个导航点间完成仪表飞行并安全返场，不加入敌军。

使用项目中实际可用的数据核实地图、机场、单位、载荷和坐标，生成对应的 .miz 并完成验证。
----

==== 启动与起飞

===== 冷启动

[source,text]
----
生成一个 JF-17 在内华达 Nellis 的冷启动练习。包括完整停机坪启动、滑行、起飞、一个短航线和返场，地面与空中交通保持简单，不设置武器目标。

不要凭记忆编造 DCS 内部名称；请生成任务文件、简报和验证结果。
----

===== 热启动

[source,text]
----
给我一个 F-14B 热启动快速任务，双人机组从 Marianas 的陆上机场停机位出发。起飞后完成一次基础导航和编队转弯，再返回同一机场，全程约 30 分钟。

请先查询真实可用的数据，生成后检查出生点、航路、载荷、触发条件和目标逻辑。
----

===== 跑道起飞

[source,text]
----
制作一个 Su-25T 从 Vaziani 跑道就绪起飞的简短任务。玩家起飞后爬升到指定高度，飞过一个训练区航点并直接返场，重点是起飞和基础操纵。

使用项目中实际可用的数据核实地图、机场、单位、载荷和坐标，生成对应的 .miz 并完成验证。
----

==== 导航与着陆

===== 目视着陆

[source,text]
----
生成一个 M-2000C 的日间目视着陆练习，玩家在高加索某机场外约 15 海里空中出生，进入标准起落航线并完成全停着陆。天气良好，加入可重复练习的无线电和航路提示。

不要凭记忆编造 DCS 内部名称；请生成任务文件、简报和验证结果。
----

===== 仪表着陆

[source,text]
----
我需要一个 JF-17 仪表着陆任务。高加索机场被低云和小雨覆盖，玩家从云上开始，依靠航电和机场进近设施完成下降、截获进近并着陆，不设置战斗。

请先查询真实可用的数据，生成后检查出生点、航路、载荷、触发条件和目标逻辑。
----

===== 夜间或侧风着陆

[source,text]
----
制作一个 Sinai 地图的 F-16C 夜间侧风着陆任务。玩家油量适中、从机场下风方向进入，能见度尚可但阵风明显，任务应允许复飞后再次进近。

使用项目中实际可用的数据核实地图、机场、单位、载荷和坐标，生成对应的 .miz 并完成验证。
----

==== 编队飞行

[source,text]
----
生成一个内华达训练空域的四机 F-5E 编队飞行任务，玩家担任二号机。编队完成集合、队形变换、战术转弯和解散返场，不设置敌机。

不要凭记忆编造 DCS 内部名称；请生成任务文件、简报和验证结果。
----

==== 空中加油

[source,text]
----
给我一个波斯湾 F/A-18C 空中加油练习，玩家从空中开始，在预警机引导下找到加油机，完成受油后返航航母。使用白天平静天气，并提供足够燃油供多次尝试。

请先查询真实可用的数据，生成后检查出生点、航路、载荷、触发条件和目标逻辑。
----

==== 航母起降

[source,text]
----
制作一个 Marianas 地图的 F-14B 航母起降快速任务。玩家在甲板热启动，完成弹射、短航线、Case I 进场和拦阻着舰，航母编队保持迎风航行。

使用项目中实际可用的数据核实地图、机场、单位、载荷和坐标，生成对应的 .miz 并完成验证。
----

==== 直升机基础

===== 悬停

[source,text]
----
生成一个 UH-1H 悬停练习，地点是高加索平坦训练场。设置几个间距不同的标志点，让玩家依次完成定点悬停、侧移、后退和原地转向，任务不包含敌人。

不要凭记忆编造 DCS 内部名称；请生成任务文件、简报和验证结果。
----

===== 贴地飞行

[source,text]
----
做一个 Syria 地图的 Mi-24P 贴地飞行任务。玩家沿山谷和村庄之间的低空航线飞行，避开已知观察哨但不发生交战，最后在前线机场着陆。

请先查询真实可用的数据，生成后检查出生点、航路、载荷、触发条件和目标逻辑。
----

===== 狭小场地着陆

[source,text]
----
生成一个 Afghanistan 山区的 CH-47F 狭小场地着陆练习。玩家需要把直升机降到三个逐渐更困难的山谷着陆区，考虑高温、高海拔和载重影响。

使用项目中实际可用的数据核实地图、机场、单位、载荷和坐标，生成对应的 .miz 并完成验证。
----

=== 近距空战（BFM/ACM）

==== 机炮

===== 1v1

[source,text]
----
生成一个全模拟 M-2000C 对全模拟 MiG-29A 的 1v1 机炮格斗任务。双方在高加索训练空域等高、等速、迎头接近，只挂训练所需的机炮弹药，玩家可以选择任一机型并在被击落后快速重开。

不要凭记忆编造 DCS 内部名称；请生成任务文件、简报和验证结果。
----

===== 2v2

[source,text]
----
制作一个 F-5E 对 MiG-21bis 的 2v2 机炮格斗。双方从中高度相向进入，AI 僚机水平相当，禁止导弹，并设置清晰的交战区边界和胜负判定。

请先查询真实可用的数据，生成后检查出生点、航路、载荷、触发条件和目标逻辑。
----

===== 1v2

[source,text]
----
给我一个 F-16C 对两架 F-5E 的 1v2 机炮训练。玩家拥有高度优势但油量有限，两架敌机协同水平中等，任务目标是在不离开训练区的情况下存活并击落两机。

使用项目中实际可用的数据核实地图、机场、单位、载荷和坐标，生成对应的 .miz 并完成验证。
----

==== 红外弹

===== 1v1

[source,text]
----
生成一个 F/A-18C 对 MiG-29A 的 1v1 红外弹格斗任务。双方各带两枚近距红外弹和机炮，从中高度视觉接触开始，不使用雷达制导导弹。

不要凭记忆编造 DCS 内部名称；请生成任务文件、简报和验证结果。
----

===== 2v2

[source,text]
----
制作一个两架 Mirage F1 对两架 F-5E 的近距红外弹训练，背景为 1980 年代 Nevada 红旗演习。双方装备符合年代，加入有限机炮弹药和可识别的训练阵营。

请先查询真实可用的数据，生成后检查出生点、航路、载荷、触发条件和目标逻辑。
----

===== 1v2

[source,text]
----
生成一个 MiG-29A 对两架 F-16C 的近距 1v2 任务。玩家只有两枚 R-73 和机炮，敌机分散在两个方位进入，重点训练目视搜索、威胁排序和脱离。

使用项目中实际可用的数据核实地图、机场、单位、载荷和坐标，生成对应的 .miz 并完成验证。
----

==== 全挂载突然遭遇

[source,text]
----
做一个两机 F-14B 与两机 MiG-29A 在低云层上下突然相遇的快速任务。双方保留典型空战挂载，但初始距离很近，预警时间很短，要求 AI 遵守合理交战规则。

不要凭记忆编造 DCS 内部名称；请生成任务文件、简报和验证结果。
----

=== 超视距空战（BVR）

==== 1v1

[source,text]
----
生成一个 F-16C 对 JF-17 的 1v1 BVR 训练，地点在波斯湾海上空域。双方从 45 海里开始，各带中距弹和近距自卫弹，胜利条件是先获得有效击落并安全脱离。

请先查询真实可用的数据，生成后检查出生点、航路、载荷、触发条件和目标逻辑。
----

==== 2v2

[source,text]
----
制作一个两架 F-14B 对两架 MiG-29A 的 2v2 超视距任务。使用开阔海域、中等云层和无第三方干扰，重点训练编队间距、目标分配和发射后防御。

使用项目中实际可用的数据核实地图、机场、单位、载荷和坐标，生成对应的 .miz 并完成验证。
----

==== 1v2

[source,text]
----
给我一个 M-2000C 对两架较弱 AI 战斗机的 1v2 BVR 挑战。玩家需要利用高度、速度和半主动雷达弹限制，在敌机形成夹击前完成一次攻击并脱离。

不要凭记忆编造 DCS 内部名称；请生成任务文件、简报和验证结果。
----

==== 4v4 预警机与数据链协同

[source,text]
----
生成一个 4v4 F/A-18C 对 Su-27 的编队 BVR 任务。双方都有预警机支援，蓝方使用数据链进行目标分配，交战区远离中立航线，并在简报中说明频道和控制信息。

请先查询真实可用的数据，生成后检查出生点、航路、载荷、触发条件和目标逻辑。
----

==== 防御脱离

[source,text]
----
制作一个 JF-17 的 BVR 防御脱离训练。玩家开始时已经被两架敌机雷达锁定并面临远距来弹，需要使用态势感知、电子对抗、地形和燃油管理脱离，而不是必须反击。

使用项目中实际可用的数据核实地图、机场、单位、载荷和坐标，生成对应的 .miz 并完成验证。
----

=== 对地武器训练

==== 航炮扫射

[source,text]
----
生成一个 A-10C II 航炮靶场任务，设置静止卡车、装甲车和带明显标记的靶标三组目标。玩家空中开始，有多次攻击机会，地面单位不还击。

不要凭记忆编造 DCS 内部名称；请生成任务文件、简报和验证结果。
----

==== 火箭弹

[source,text]
----
制作一个 Mi-24P 火箭弹训练任务，目标是荒漠靶场中的轻型车辆和区域目标。加入安全进出航线、建议攻击方向和足够弹药，不设置防空威胁。

请先查询真实可用的数据，生成后检查出生点、航路、载荷、触发条件和目标逻辑。
----

==== CCIP 非制导炸弹

[source,text]
----
给我一个 F-16C 的 CCIP 非制导炸弹练习。玩家从 Nellis 热启动，前往靶场攻击多种尺寸的圆形目标，天气晴朗并提供多次投弹和返场油量。

使用项目中实际可用的数据核实地图、机场、单位、载荷和坐标，生成对应的 .miz 并完成验证。
----

==== CCRP 非制导炸弹

[source,text]
----
生成一个 M-2000C 的 CCRP 投弹训练，玩家需要在低空进入后利用指定点对模拟跑道目标实施平飞投弹。加入简单导航和结果判定，不安排敌方拦截。

不要凭记忆编造 DCS 内部名称；请生成任务文件、简报和验证结果。
----

==== 集束弹药

[source,text]
----
制作一个 F-16C 集束弹药训练任务，目标为分散停放的软目标和轻装甲车。允许玩家比较两种投放高度，并确保目标区远离友军和居民区。

请先查询真实可用的数据，生成后检查出生点、航路、载荷、触发条件和目标逻辑。
----

==== 激光制导武器

[source,text]
----
生成一个 F/A-18C 激光制导炸弹练习，地面 JTAC 为两座不同目标提供激光指示。玩家从航母热启动，需要正确联络、识别目标、投弹并返航。

使用项目中实际可用的数据核实地图、机场、单位、载荷和坐标，生成对应的 .miz 并完成验证。
----

==== 电视或光电制导武器

[source,text]
----
做一个 JF-17 光电制导武器任务，目标是山谷中的雷达车和桥梁。天气允许传感器工作，但地形会遮挡视线，玩家要在两种目标之间选择优先级。

不要凭记忆编造 DCS 内部名称；请生成任务文件、简报和验证结果。
----

==== GPS/INS 制导武器

[source,text]
----
制作一个 F/A-18C JDAM 训练，简报提供三个静态目标的坐标。玩家需要在不进入短程防空圈的情况下完成多目标投放，并得到每个目标的命中结果。

请先查询真实可用的数据，生成后检查出生点、航路、载荷、触发条件和目标逻辑。
----

==== 防区外武器

[source,text]
----
生成一个 JF-17 防区外打击练习，目标为海岸附近的加固指挥所，外围有中程防空。玩家从安全距离规划发射条件，任务重点是航路、发射窗口和脱离。

使用项目中实际可用的数据核实地图、机场、单位、载荷和坐标，生成对应的 .miz 并完成验证。
----

==== 反辐射导弹

[source,text]
----
制作一个 F-16C HARM 靶场任务，设置会间歇开关机的不同雷达。玩家需要识别威胁、选择攻击方式并压制至少一部雷达，其他敌人保持训练模式。

不要凭记忆编造 DCS 内部名称；请生成任务文件、简报和验证结果。
----

==== 反舰导弹

[source,text]
----
给我一个 AJS-37 在波罗的海环境中的反舰导弹练习。目标是一支由护卫舰保护的小型运输船队，玩家低空接近、完成齐射后高速撤离，不加入敌方战斗机。

请先查询真实可用的数据，生成后检查出生点、航路、载荷、触发条件和目标逻辑。
----

=== 传感器与生存训练

==== 雷达、IRST 与数据链

[source,text]
----
生成一个 MiG-29A 传感器训练，空域中有不同高度和速度的友机、敌机与中立机。玩家需要使用雷达、IRST 和数据链建立态势并正确分类目标，不允许开火。

使用项目中实际可用的数据核实地图、机场、单位、载荷和坐标，生成对应的 .miz 并完成验证。
----

==== 吊舱与 JTAC

[source,text]
----
制作一个 A-10C II 吊舱与 JTAC 协同任务。地面控制员依次提供九线、坐标和激光指示，玩家只做目标查找和模拟攻击，不需要释放实弹。

不要凭记忆编造 DCS 内部名称；请生成任务文件、简报和验证结果。
----

==== RWR、ECM 与干扰弹

[source,text]
----
生成一个 F/A-18C 防御系统训练。玩家沿固定航线依次遭遇搜索雷达、跟踪雷达和模拟导弹攻击，需要正确使用 RWR、电子干扰和箔条热焰弹后返航。

请先查询真实可用的数据，生成后检查出生点、航路、载荷、触发条件和目标逻辑。
----

==== 低空突防

[source,text]
----
制作一个 AJS-37 低空突防任务，从沿海机场起飞后沿山谷和海岸飞到目标区，不投放武器。计分依据是航线保持、暴露时间和准时到达。

使用项目中实际可用的数据核实地图、机场、单位、载荷和坐标，生成对应的 .miz 并完成验证。
----

==== 紧急返航

[source,text]
----
给我一个 F-14B 紧急返航场景。玩家在海上巡逻时发生单发故障并伴随液压异常，需要判断是否抛弃挂载、联络航母并完成安全着舰或备降。

不要凭记忆编造 DCS 内部名称；请生成任务文件、简报和验证结果。
----

=== 特技飞行

==== 单机特技

[source,text]
----
生成一个 M-2000C 单机特技任务，地点为 Nevada 干湖上空。设置连续的进入点，依次练习筋斗、横滚、古巴八字和低空高速通场，禁止敌方单位。

请先查询真实可用的数据，生成后检查出生点、航路、载荷、触发条件和目标逻辑。
----

==== 编队特技

[source,text]
----
制作一个四架 L-39 的编队特技场景，玩家担任长机，AI 僚机保持稳定队形。包括起飞集合、菱形队形动作、拉烟通场和编队着陆。

使用项目中实际可用的数据核实地图、机场、单位、载荷和坐标，生成对应的 .miz 并完成验证。
----

== 战斗场景

=== 制空与防空

==== 紧急起飞拦截

[source,text]
----
生成一个 1988 年德国冷战地图的 MiG-29A 紧急起飞拦截任务。玩家与 AI 僚机冷启动，法国打击编队正从西侧接近东柏林，必须在其进入武器释放区前完成拦截；加入地面引导和 A-50 支援。

不要凭记忆编造 DCS 内部名称；请生成任务文件、简报和验证结果。
----

==== 打击编队拦截

[source,text]
----
制作一个 F-14B 拦截远程轰炸机与护航机的波斯湾任务。双机从航母弹射起飞，在预警机引导下识别并攻击来袭编队，同时避免误伤附近中立航班。

请先查询真实可用的数据，生成后检查出生点、航路、载荷、触发条件和目标逻辑。
----

==== BARCAP

[source,text]
----
生成一个双人 F-16C BARCAP 任务，保护高加索前线机场和补给节点。巡逻持续约 60 分钟，敌方可能分批派出攻击机与战斗机，蓝方有加油机但弹药补给有限。

使用项目中实际可用的数据核实地图、机场、单位、载荷和坐标，生成对应的 .miz 并完成验证。
----

==== TARCAP

[source,text]
----
制作一个 F/A-18C TARCAP 场景，为深入敌后的打击编队保护目标区上空。玩家四机需要提前清理敌方 CAP、在攻击机投弹期间维持空优，并掩护其撤离。

不要凭记忆编造 DCS 内部名称；请生成任务文件、简报和验证结果。
----

==== 战斗机扫荡

[source,text]
----
生成一个 Syria 地图的 F-14B 战斗机扫荡任务。两架玩家飞机先于主攻击群进入，诱使并消灭敌方巡逻机，限制交战区和燃油，使玩家不能无限追击。

请先查询真实可用的数据，生成后检查出生点、航路、载荷、触发条件和目标逻辑。
----

==== 护航

[source,text]
----
制作一个四机 M-2000C 护航任务，保护低空飞行的 Mirage F1 打击编队穿越敌方空域。威胁包括前方截击机和返程追击，护航机必须优先保证攻击机存活。

使用项目中实际可用的数据核实地图、机场、单位、载荷和坐标，生成对应的 .miz 并完成验证。
----

==== 基地防空

[source,text]
----
生成一个 1970 年代 F-5E 基地防空任务。玩家从遭到空袭警报的机场紧急起飞，对付低空来袭的攻击机，同时机场防空炮火和跑道受损会影响返场。

不要凭记忆编造 DCS 内部名称；请生成任务文件、简报和验证结果。
----

==== 舰队防空

[source,text]
----
制作一个 F-14A 舰队防空任务，敌方轰炸机从多个方位向航母编队发射反舰导弹。玩家需在远距截击轰炸机，并在漏网时协助处理来袭导弹。

请先查询真实可用的数据，生成后检查出生点、航路、载荷、触发条件和目标逻辑。
----

==== OCA 敌机压制

[source,text]
----
生成一个 F-16C 进攻性制空任务，目标是在敌方机场起飞架次形成前压制其战斗机。玩家编队需要同时处理空中警戒机和停机区目标，但不得攻击民用机场设施。

使用项目中实际可用的数据核实地图、机场、单位、载荷和坐标，生成对应的 .miz 并完成验证。
----

=== 对地作战

==== 预先规划 CAS

[source,text]
----
制作一个 A-10C II 预先规划 CAS 任务，友军正在防守高加索山口。简报给出目标区、友军位置和预定攻击时间，玩家双机负责摧毁已知装甲集群并按时撤离。

不要凭记忆编造 DCS 内部名称；请生成任务文件、简报和验证结果。
----

==== 临机 CAS

[source,text]
----
生成一个双人 AH-64D 临机 CAS 场景。玩家在待命区盘旋，地面部队遭遇突发攻击后通过 JTAC 提供九线，目标会移动且靠近友军，必须确认后才能开火。

请先查询真实可用的数据，生成后检查出生点、航路、载荷、触发条件和目标逻辑。
----

==== BAI 战场遮断

[source,text]
----
制作一个 F-16C 战场遮断任务，敌方装甲增援正沿两条道路前往前线。玩家需要根据侦察信息选择拦截点，在车队进入友军接触区前摧毁关键车辆。

使用项目中实际可用的数据核实地图、机场、单位、载荷和坐标，生成对应的 .miz 并完成验证。
----

==== 武装侦察

[source,text]
----
生成一个 OH-58D 武装侦察任务，搜索 Syria 山区中疑似导弹小组和补给车。目标位置不完全确定，玩家需要观察、标记并只攻击确认的敌对单位。

不要凭记忆编造 DCS 内部名称；请生成任务文件、简报和验证结果。
----

==== 精确打击

[source,text]
----
制作一个 JF-17 精确打击任务，目标是城市边缘的一座军用通信中心。附近有民用建筑和短程防空，玩家必须使用合适武器和攻击方向控制附带损害。

请先查询真实可用的数据，生成后检查出生点、航路、载荷、触发条件和目标逻辑。
----

==== 深入打击

[source,text]
----
生成一个四机 F-15E 深入打击场景，目标是敌后山谷中的地下设施入口。加入护航、SEAD 和加油支援，任务总时长约 100 分钟，敌方会在返程组织拦截。

使用项目中实际可用的数据核实地图、机场、单位、载荷和坐标，生成对应的 .miz 并完成验证。
----

==== 机场与跑道攻击

[source,text]
----
制作一个 AJS-37 低空机场打击任务，目标是敌方跑道和停放在军用区域的攻击机。玩家需要在跑道修复前造成有效破坏，并避免攻击共用机场的民用部分。

不要凭记忆编造 DCS 内部名称；请生成任务文件、简报和验证结果。
----

==== 装甲集群攻击

[source,text]
----
生成一个 Su-25T 对大型装甲集群的任务。敌军有自行高炮和短程防空保护，蓝方提供侦察无人机但没有制空权，玩家需要选择攻击次序并保留返航弹药。

请先查询真实可用的数据，生成后检查出生点、航路、载荷、触发条件和目标逻辑。
----

==== 固定设施攻击

[source,text]
----
制作一个 F/A-18C 打击桥梁和铁路枢纽的任务，以阻止敌方后勤运输。两个目标不能同时轻易摧毁，玩家编队需要分配武器，并在简报中说明对战场的预期影响。

使用项目中实际可用的数据核实地图、机场、单位、载荷和坐标，生成对应的 .miz 并完成验证。
----

=== 防空压制

==== SEAD

[source,text]
----
生成一个双人 F-16C SEAD 任务，为随后进入的攻击机压制一套中程防空系统。敌方雷达会关机并有近程系统掩护，成功条件是在打击窗口内让威胁无法有效射击。

不要凭记忆编造 DCS 内部名称；请生成任务文件、简报和验证结果。
----

==== DEAD

[source,text]
----
制作一个 F/A-18C DEAD 任务，目标是彻底摧毁沿海综合防空阵地的雷达、发射车和指挥节点。玩家有一次加油机会，敌方会部署诱饵和机动备用雷达。

请先查询真实可用的数据，生成后检查出生点、航路、载荷、触发条件和目标逻辑。
----

==== SEAD 护航

[source,text]
----
生成一个 F-16C SEAD 护航场景，玩家伴随 F-15E 打击编队穿过多个防空威胁区。重点是按打击群节奏响应新出现的雷达，而不是提前单独猎杀所有目标。

使用项目中实际可用的数据核实地图、机场、单位、载荷和坐标，生成对应的 .miz 并完成验证。
----

==== SEAD 扫荡

[source,text]
----
制作一个两机 F/A-18C 的 SEAD 扫荡任务，敌方机动防空单位分散在可能的行军路线旁。玩家要主动搜索、诱使其开机并逐个清除，为次日的大行动开辟通道。

不要凭记忆编造 DCS 内部名称；请生成任务文件、简报和验证结果。
----

=== 海上作战

==== 反舰打击

[source,text]
----
生成一个 AJS-37 四机反舰打击任务，目标是北海中的护卫舰编队。玩家需要低空导航、协调导弹齐射并避开舰载防空，蓝方提供海上侦察但没有空中加油。

请先查询真实可用的数据，生成后检查出生点、航路、载荷、触发条件和目标逻辑。
----

==== 沿海目标打击

[source,text]
----
制作一个 F/A-18C 沿海打击任务，摧毁港口内的导弹艇、燃料库和岸防雷达。目标靠近城市，玩家必须区分军用码头并控制攻击规模。

使用项目中实际可用的数据核实地图、机场、单位、载荷和坐标，生成对应的 .miz 并完成验证。
----

==== 海上运输队护航

[source,text]
----
生成一个双人 F-14B 海上运输队护航任务。友军货船要穿过波斯湾狭窄水道，威胁包括低空攻击机和反舰导弹艇，玩家需要与舰队防空协同保护船队。

不要凭记忆编造 DCS 内部名称；请生成任务文件、简报和验证结果。
----

==== 舰队防空

[source,text]
----
制作一个多人舰队防空场景，四架 F/A-18C 从航母起飞，抵御两波不同高度和方向的反舰攻击。加入 E-2、加油机和舰载防空，但不要让舰船自动解决全部威胁。

请先查询真实可用的数据，生成后检查出生点、航路、载荷、触发条件和目标逻辑。
----

==== 两栖行动支援

[source,text]
----
生成一个 AV-8B 支援两栖登陆的任务。玩家从两栖攻击舰起飞，先压制海岸火力，再为登陆部队提供临机支援；敌方有机动防空和装甲预备队。

使用项目中实际可用的数据核实地图、机场、单位、载荷和坐标，生成对应的 .miz 并完成验证。
----

=== 直升机与特种行动

==== 空中突击

[source,text]
----
制作一个 Mi-8MTV2 空中突击任务，多架运输直升机在武装护航下把步兵送到山口据点。玩家担任编队长，负责航线、着陆时序和撤离信号。

不要凭记忆编造 DCS 内部名称；请生成任务文件、简报和验证结果。
----

==== 部队投送

[source,text]
----
生成一个 UH-1H 夜间部队投送任务，把特种部队送到 Syria 敌后村庄附近。使用低空隐蔽航线、有限夜视条件和严格的着陆时间窗，避免提前惊动守军。

请先查询真实可用的数据，生成后检查出生点、航路、载荷、触发条件和目标逻辑。
----

==== 部队撤离

[source,text]
----
制作一个 Mi-8 撤离被包围侦察小组的任务。着陆区会在任务中改变，敌方地面火力逐渐逼近，玩家需要根据无线电更新选择备用点并安全返航。

使用项目中实际可用的数据核实地图、机场、单位、载荷和坐标，生成对应的 .miz 并完成验证。
----

==== 货物运输

[source,text]
----
生成一个 CH-47F 重型货物运输任务，把火炮和弹药从后方基地运到前线山地阵地。考虑外挂载荷、天气和高海拔性能，并对货物完整送达进行判定。

不要凭记忆编造 DCS 内部名称；请生成任务文件、简报和验证结果。
----

==== 战场补给

[source,text]
----
制作一个 UH-1H 战场补给任务，连续向三个分散阵地运送弹药和医疗物资。部分航线受到轻武器威胁，玩家可以选择速度更快或更隐蔽的路线。

请先查询真实可用的数据，生成后检查出生点、航路、载荷、触发条件和目标逻辑。
----

==== 战斗搜救

[source,text]
----
生成一个双机 UH-1H 战斗搜救任务，营救在敌后跳伞的飞行员。一架负责救援、一架提供掩护，敌军搜索队会接近坠落区域，蓝方有短时战斗机掩护。

使用项目中实际可用的数据核实地图、机场、单位、载荷和坐标，生成对应的 .miz 并完成验证。
----

==== 直升机护航

[source,text]
----
制作一个 AH-64D 护航运输直升机群的任务。航线经过两个容易伏击的山谷，敌方可能使用高炮、便携防空和武装车辆，玩家必须保持与运输队的合理距离。

不要凭记忆编造 DCS 内部名称；请生成任务文件、简报和验证结果。
----

==== 直升机猎杀

[source,text]
----
生成一个 Mi-24P 猎杀敌方直升机和前进基地的任务。目标会在低空利用地形机动，玩家必须先找到其补给点，再决定拦截空中目标还是攻击基地。

请先查询真实可用的数据，生成后检查出生点、航路、载荷、触发条件和目标逻辑。
----

=== 侦察与支援

==== 战术侦察

[source,text]
----
制作一个 Mirage F1 战术侦察任务，沿敌方边境拍摄三个疑似部署区。玩家需要保持指定高度和航向完成侦察，同时避开巡逻机和短程防空，带回全部情报才算成功。

使用项目中实际可用的数据核实地图、机场、单位、载荷和坐标，生成对应的 .miz 并完成验证。
----

==== AFAC/JTAC

[source,text]
----
生成一个双人 A-10C II 空中前进引导任务。玩家一机负责发现和标记目标，另一机根据引导攻击；友军靠近目标区，必须使用九线、激光码和明确的交战许可。

不要凭记忆编造 DCS 内部名称；请生成任务文件、简报和验证结果。
----

==== 预警机支援

[source,text]
----
制作一个 F-16C 保护预警机并利用其指挥完成拦截的任务。敌方会尝试绕开 CAP 攻击 A-50/E-3，玩家需要在保护高价值目标和追击敌机之间做取舍。

请先查询真实可用的数据，生成后检查出生点、航路、载荷、触发条件和目标逻辑。
----

==== 空中加油支援

[source,text]
----
生成一个长航程 F-14B 作战任务，玩家必须在去程或返程与加油机会做选择。敌方可能威胁加油机，任务应让燃油计划、会合时间和护航成为关键。

使用项目中实际可用的数据核实地图、机场、单位、载荷和坐标，生成对应的 .miz 并完成验证。
----

==== 回收加油机

[source,text]
----
制作一个航母回收支援场景。玩家 F/A-18C 编队在夜间返航时有两机低油，航母附近安排回收加油机；天气恶化，必须合理分配受油顺序并完成着舰。

不要凭记忆编造 DCS 内部名称；请生成任务文件、简报和验证结果。
----

==== 战场监视

[source,text]
----
生成一个 OH-58D 战场监视任务，观察敌方车队、炮兵和防空单位的移动并向友军报告。玩家不以摧毁目标为主，要在不暴露的情况下保持接触并更新坐标。

请先查询真实可用的数据，生成后检查出生点、航路、载荷、触发条件和目标逻辑。
----

==== 武力展示

[source,text]
----
制作一个双机 F/A-18C 武力展示任务，阻止敌对地面部队接近停火区。玩家可以低空通场和投放热焰弹，但除非遭到攻击不得开火，并需要识别违规单位。

使用项目中实际可用的数据核实地图、机场、单位、载荷和坐标，生成对应的 .miz 并完成验证。
----

=== 地面与联合行动

==== 阵地防御

[source,text]
----
生成一个 Combined Arms 阵地防御场景，玩家指挥装甲、反坦克和防空单位守住关键路口，友军 A-10C 可提供有限支援。敌人分阶段进攻，弹药和增援都有限。

不要凭记忆编造 DCS 内部名称；请生成任务文件、简报和验证结果。
----

==== 占领据点

[source,text]
----
制作一个地空联合占领据点任务。地面玩家组织装甲进攻，直升机玩家运送步兵到侧翼，固定翼玩家压制防空；只有地面部队实际控制目标区才算成功。

请先查询真实可用的数据，生成后检查出生点、航路、载荷、触发条件和目标逻辑。
----

==== 地面进攻

[source,text]
----
生成一个大规模地面进攻场景，玩家作为 Combined Arms 指挥官协调侦察、炮兵、装甲和陆航。敌方有纵深防御和预备队，任务持续约 90 分钟并允许多条推进路线。

使用项目中实际可用的数据核实地图、机场、单位、载荷和坐标，生成对应的 .miz 并完成验证。
----

==== 车队护送

[source,text]
----
制作一个 Ka-50 护送补给车队穿越危险道路的任务。威胁包括伏击小组、地雷区和零散防空，车队可以停车或改道，玩家要保证关键车辆抵达。

不要凭记忆编造 DCS 内部名称；请生成任务文件、简报和验证结果。
----

==== 车队截击

[source,text]
----
生成一个 F-16C 截击高价值车队的任务。车队混在普通交通中并可能分散，玩家需要等待确认后打击指挥车和防空车辆，不能攻击民用车辆。

请先查询真实可用的数据，生成后检查出生点、航路、载荷、触发条件和目标逻辑。
----

==== 联合火力支援

[source,text]
----
制作一个联合火力支援任务，F/A-18C、A-10C、地面炮兵和 JTAC 围绕同一前线目标协同。设置时间分隔、空域限制和友军位置，避免各支援单元互相冲突。

使用项目中实际可用的数据核实地图、机场、单位、载荷和坐标，生成对应的 .miz 并完成验证。
----

==== 大型多任务编队

[source,text]
----
生成一个大型多人打击行动，包含制空、SEAD、护航、主打击、反舰和加油编队，目标是瘫痪沿海基地群。每个玩家小队有独立任务和时间线，但最终目标相互依赖。

不要凭记忆编造 DCS 内部名称；请生成任务文件、简报和验证结果。
----

=== 多人场景

==== 合作任务

[source,text]
----
制作一个四人合作 F-16C 任务，两人负责 SEAD，两人攻击敌方指挥所。所有玩家冷启动并共用简报、无线电和加油支援，任务难度适合固定小队在 75 分钟内完成。

请先查询真实可用的数据，生成后检查出生点、航路、载荷、触发条件和目标逻辑。
----

==== 红蓝对抗

[source,text]
----
生成一个 8v8 红蓝对抗场景，双方各有两个机场、预警机和有限重生次数。目标不只是击落敌机，还要保护己方补给航班并攻击对方雷达，确保两边条件对称。

使用项目中实际可用的数据核实地图、机场、单位、载荷和坐标，生成对应的 .miz 并完成验证。
----

==== 人类 JTAC 协同

[source,text]
----
制作一个多人 CAS 任务，由一名 Combined Arms 玩家担任 JTAC，其他玩家驾驶 A-10C II 和 F/A-18C。地面战况会变化，只有人类 JTAC 能确认目标和授权攻击。

不要凭记忆编造 DCS 内部名称；请生成任务文件、简报和验证结果。
----

==== Combined Arms 地空协同

[source,text]
----
生成一个地空协同多人场景，地面玩家控制防守部队和防空，飞行玩家提供侦察、运输和火力支援。胜利需要守住两个区域并完成一次反击，任何单一兵种都不能独立获胜。

请先查询真实可用的数据，生成后检查出生点、航路、载荷、触发条件和目标逻辑。
----

== 战役

=== 训练与资格战役

==== 基础飞行资格

[source,text]
----
设计一个 M-2000C 基础飞行资格战役，共 6 个关联任务，依次考核冷启动、滑行起飞、导航、编队、仪表进近和紧急返航。每关有明确通过标准，失败可以重飞但保留成绩记录。

使用项目中实际可用的数据核实地图、机场、单位、载荷和坐标，生成对应的 .miz 并完成验证。
----

==== 作战资格

[source,text]
----
生成一个 F-16C 作战资格战役，从机炮和红外弹训练开始，逐步加入 BVR、对地武器、SEAD 和双机综合考核。任务难度递增，最后一关必须结合前面学到的全部能力。

不要凭记忆编造 DCS 内部名称；请生成任务文件、简报和验证结果。
----

=== 短战役

==== 线性迷你战役

[source,text]
----
制作一个五关 F-5E 线性迷你战役，背景为边境冲突升级。任务从巡逻和识别开始，经过拦截、护航和机场防御，最后执行一次有限反击；每关约 40 分钟。

请先查询真实可用的数据，生成后检查出生点、航路、载荷、触发条件和目标逻辑。
----

==== 历史事件短战役

[source,text]
----
生成一个 Normandy 地图的 P-51D 历史短战役，围绕同一周内的三次行动展开：护航轰炸机、攻击交通线和防御机场。装备、阵营和天气参考对应年代，但故事人物可以虚构。

使用项目中实际可用的数据核实地图、机场、单位、载荷和坐标，生成对应的 .miz 并完成验证。
----

=== 叙事战役

==== 线性虚构战役

[source,text]
----
设计一个 F/A-18C 线性虚构战役，玩家航母战斗群介入波斯湾危机。用 10 个任务讲完整故事，从威慑巡逻发展到联合打击和停火保护，人物与无线电事件要有连续性。

不要凭记忆编造 DCS 内部名称；请生成任务文件、简报和验证结果。
----

==== 线性历史战役

[source,text]
----
制作一个 F-4E 冷战历史背景战役，时间固定在 1980 年代德国，包含低空打击、护航、拦截和侦察。不要使用超出年代的装备，任务顺序按战局时间线推进。

请先查询真实可用的数据，生成后检查出生点、航路、载荷、触发条件和目标逻辑。
----

==== 角色驱动战役

[source,text]
----
生成一个 Mi-24P Afghanistan 角色驱动战役。玩家与同一批机组、地面指挥员和运输单位共同经历护航、救援和山地突击，人物伤亡与选择会在后续简报和对话中体现。

使用项目中实际可用的数据核实地图、机场、单位、载荷和坐标，生成对应的 .miz 并完成验证。
----

=== 分支战役

==== 根据任务成败分支

[source,text]
----
制作一个 JF-17 分支战役，每关胜败都会改变下一任务：摧毁雷达可降低后续防空，护航失败会减少可用攻击机，机场受损则迫使玩家转场。至少设计两条可完成的路线。

不要凭记忆编造 DCS 内部名称；请生成任务文件、简报和验证结果。
----

==== 多结局

[source,text]
----
生成一个 F-14B 多结局战役，玩家的拦截判断、对中立目标的克制和友军保护情况影响外交局势。设计胜利、有限成功和失败三种结局，不要只用击落数量决定。

请先查询真实可用的数据，生成后检查出生点、航路、载荷、触发条件和目标逻辑。
----

=== 持续战役

==== 保存损失与资源

[source,text]
----
设计一个 A-10C II 持续战役，保存玩家飞机、AI 僚机、武器库存和地面部队损失。玩家可以选择任务和补给优先级，过度消耗精确武器会影响后续关卡。

使用项目中实际可用的数据核实地图、机场、单位、载荷和坐标，生成对应的 .miz 并完成验证。
----

==== 前线推进

[source,text]
----
生成一个德国冷战地图的 F-16C 前线推进战役。双方地面控制区随战果变化，新的机场、目标和巡逻区逐步开放，空中任务必须真实反映当前前线位置。

不要凭记忆编造 DCS 内部名称；请生成任务文件、简报和验证结果。
----

==== 渐进式长期战役

[source,text]
----
制作一个持续 25 个以上任务的多机型长期战役，从局部边境事件发展成联合战争。保存兵力和后勤状态，允许玩家在制空、打击、直升机和支援岗位之间选择。

请先查询真实可用的数据，生成后检查出生点、航路、载荷、触发条件和目标逻辑。
----

=== 动态战役

==== 动态任务生成

[source,text]
----
设计一个波斯湾动态战役，每个回合根据现有目标、兵力、损失和情报自动生成不同任务包。玩家可以接受或拒绝任务，但时间推进后敌方也会执行自己的行动。

使用项目中实际可用的数据核实地图、机场、单位、载荷和坐标，生成对应的 .miz 并完成验证。
----

==== 动态战区

[source,text]
----
生成一个科拉地图的动态战区，包含可变化的前线、海上运输、机场控制、雷达覆盖和补给网络。空中、地面和海上行动应互相影响，战区状态在任务之间持续保存。

不要凭记忆编造 DCS 内部名称；请生成任务文件、简报和验证结果。
----

==== 动态合作战役

[source,text]
----
制作一个 Syria 地图的四到八人动态合作战役。玩家小队可以分别承担制空、打击、直升机运输和 Combined Arms 指挥，服务器重启后保存战区进度，并能补充缺席玩家的 AI。

请先查询真实可用的数据，生成后检查出生点、航路、载荷、触发条件和目标逻辑。
----
